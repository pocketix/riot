query SingleUserDefinedDeviceType($input: ID!) {
    singleUserDefinedDeviceType(input: $input) {
        id
        denotation
        parameters {
            id
            name
            type
        }
    }
}
