apiVersion: v1
kind: Service
metadata:
  name: riot-backend-core-service
  namespace: riot
spec:
  type: ClusterIP
  selector:
    app: riot-backend-core
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP
