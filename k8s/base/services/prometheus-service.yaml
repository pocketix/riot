apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: riot
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9090"
spec:
  type: NodePort
  ports:
    - port: 9091
      targetPort: 9090
      nodePort: 30090
