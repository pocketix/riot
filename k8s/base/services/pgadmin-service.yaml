apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: riot
spec:
  selector:
    app: pgadmin
  type: NodePort
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 80
      nodePort: 30081
