query RestOfKPIDefinitionDetailPageData {
    sdTypes {
        id
        denotation
        parameters {
            id
            denotation
            type
        }
    }
    sdInstances {
        id
        uid
        confirmedByUser
        userIdentifier
        type {
            id
        }
    }
}
