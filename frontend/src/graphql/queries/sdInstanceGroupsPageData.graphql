query SDInstanceGroupsPageData {
    sdInstances {
        id
        confirmedByUser
        userIdentifier
        type {
            id
        }
    }
    sdInstanceGroups {
        id
        userIdentifier
        sdInstanceIDs
    }
    kpiDefinitions {
        id
        userIdentifier
        sdTypeID
    }
    kpiFulfillmentCheckResults {
        kpiDefinitionID
        sdInstanceID
        fulfilled
    }
}
